enum Routes {
  HOME = '/',
  GUESTS = '#guests',
  SHIRT = '#shirt',
  FAQS = '/faqs',
  EVENTS = '#events',
  CONTACT_US = '/contact-us',
  UPDATES = '/updates',
  SUPPORT = '/support',
}

export default Routes;
